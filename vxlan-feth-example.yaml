server:
  listen: "0.0.0.0:4789"
  vni: 100
  # api: "/tmp/vxlan-feth.sock"  # Optional; inspect API unix socket path (default: /tmp/vxlan-feth.sock)

interface:
  io:
    unit: 101
    mtu: 1450
    # mac: "02:aa:bb:cc:dd:ee"  # Optional; random if omitted
  ip:
    unit: 100
    address: "10.0.0.2/24"
    mtu: 1450
    # mac: "02:aa:bb:cc:dd:ee"  # Optional; random if omitted

fdb:
  # BUM entry — flood broadcast/unknown-unicast/multicast to these VTEPs
  - mac: "00:00:00:00:00:00"
    dst: "192.168.50.81:4789"
  - mac: "00:00:00:00:00:00"
    dst: "192.168.50.212:4789"

  # Static unicast entries — known MAC-to-VTEP mappings
  # - mac: "46:69:54:77:86:17"
  #   dst: "192.168.50.212:4789"
